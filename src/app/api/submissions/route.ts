import { NextResponse } from "next/server";
export const GET = () => NextResponse.json({ message: "Mock mode — use client context" });
export const POST = () => NextResponse.json({ message: "Mock mode — use client context" });
